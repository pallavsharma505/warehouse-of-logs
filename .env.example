# API key for authenticating log ingestion requests
API_KEY=dev-api-key

# Service ports
COLLECTOR_PORT=7001
API_PORT=7002

# Optional: custom path for the SQLite database file
# DATABASE_PATH=/path/to/logs.sqlite
